.DS_Store
.claude/

# Audio clips stay out of git for now — not ready to publish. A checkout
# without clips plays silent.
audio/**/*.wav
audio/**/*.ogg
audio/**/*.m4a
audio/**/*.mp3
audio/**/*.webm

# Lossless recording sources — every re-record of a tracked binary is a
# permanent copy in git history.
audio/_masters/
